#pragma once
#include "Common.h"

class Solution {
public:
    void rotate(vector<int>& nums, int k) {
        int N = nums.size();
        if (N == 0)
            return;
        
        k = k % N;

        
    }
};

void Test()
{

}